[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "careerrank-ai"
version = "0.1.0"
description = "Explainable personalized job recommendation and ranking system"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["app*"]
